# Archive Policy

Archived documents are historical records only. When a newer document supersedes an older one, retain the older document here when it contains useful project history, but make the newer document the only active reference in current project-control files.