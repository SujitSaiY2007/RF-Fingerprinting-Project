# Website Release Check

Repository-side final demonstrator assets are present under `ui/` and the Pages workflow is present under `.github/workflows/`.

Public deployment is intentionally not marked verified until GitHub Actions completes a successful Pages deployment after merge to `main`.
